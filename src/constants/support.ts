// Set this to your Buy Me a Coffee page (e.g. "https://buymeacoffee.com/yourname")
// to activate that button on the Support screen. Left empty for now — the button
// shows a "coming soon" state until this is filled in.
export const BUY_ME_A_COFFEE_URL: string | null = null;

// Set this to a real rewarded ad unit ID from your own AdMob account before
// release (see docs/LAUNCH_CHECKLIST.md). Left null for now — the Support
// screen falls back to Google's official TEST ad unit id (TestIds.REWARDED),
// which serves real ad creatives for development but earns no real revenue.
export const REWARDED_AD_UNIT_ID: string | null = null;
